Backups directory.

This directory is kept in the repo so scripts can write logical SQL dumps here. SQL dump files (*.sql) are ignored by .gitignore — keep backups on a private branch or external storage.
